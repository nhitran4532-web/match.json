name: Auto Update Gavang JSON

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    # Thêm dòng này để khẳng định quyền hạn một lần nữa
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          # Sử dụng Token mặc định của GitHub để có quyền Ghi
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Fetch and Transform Data
        run: |
          curl -s "https://api-gavang.gvtv1.com/v1/streams" | jq '{
            id: "gavang_pro",
            name: "GÀ VÀNG TV LIVE",
            image: { type: "cover", url: "https://gavang6.live/images/header/gavangtv-logo-v2.png" },
            groups: [{
              id: "group-live",
              name: "🔴 TRẬN ĐẤU ĐANG DIỄN RA",
              display: "vertical",
              grid_number: 2,
              channels: (if .data == null then [] else [.data[] | {
                id: ("ch-" + (.id|tostring)),
                name: ((if .is_live then "🔴 " else "🕒 " end) + .team_1 + " vs " + .team_2),
                type: "single",
                display: "thumbnail-only",
                image: { url: .team_1_logo, display: "contain" },
                sources: [{
                  id: ("src-" + (.id|tostring)),
                  name: "Gavang TV",
                  contents: [{
                    id: ("cnt-" + (.id|tostring)),
                    name: (.title // "Trực tiếp bóng đá"),
                    streams: [{
                      id: "strm-main",
                      name: "Link Chính",
                      stream_links: [{
                        id: "lnk-main",
                        name: "HLS High",
                        type: "hls",
                        url: .source_live,
                        request_headers: [
                          {key: "Referer", value: "https://gavang6.live/"},
                          {key: "User-Agent", value: "Mozilla/5.0"}
                        ]
                      }]
                    }]
                  }]
                }]
              }] end)
            }]
          }' > match.json

      - name: Save changes to GitHub
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add match.json
          # Thêm cờ xác thực để tránh lỗi quyền ghi
          git commit -m "Update match.json" || exit 0
          git push origin main
